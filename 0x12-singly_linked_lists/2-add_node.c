#include "lists.h"

/**
 * add_node_end -
 * @head:
 * @str:
 * Return:
 */

list_t *add_node(list_t **head, const char *str)
{
	list_t *new;

	new = malloc(sizeof(list_t));

	if (new == NULL)
		return(NULL);

	
}
